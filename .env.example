DB_HOST=127.0.0.1
DB_DATABASE=contact_worx
DB_USERNAME=
DB_PASSWORD=
DB_PORT=3306
DB_CHARSET=utf8mb4
# Generate with: php -r "echo bin2hex(random_bytes(32));"
CW_TOKEN_SECRET=
# Set to 1 to show decision/score/evidence on the demo's result page. Leave unset in anything real.
CW_DEBUG=0
